extends ./../layout


block content
  div.thumbnailImages
    each photo in gallery

      div.block.eachPhotoContent
        h4 #{photo.title}

        //every photo that gets uploaded
        a(href='gallery/#{photo.id}')
          img.uploadedPhoto(src='#{photo.link}')

        //- div.photoDescription
          p #{photo.description}